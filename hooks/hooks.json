{
  "description": "Veto permission gateway hooks",
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "^(?!ExitPlanMode$|AskUserQuestion$).*",
        "hooks": [
          {
            "type": "command",
            "command": "${HOME}/.claude/plugins/marketplaces/veto-marketplace/scripts/evaluate.py",
            "timeout": 30
          }
        ]
      }
    ]
  }
}